<%= form_for [@post, @post.comments.build], remote: true do |f| %>
   <%= f.label :body %>
   <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter comment body" %>
   <div class="form-group">
    <br>
     <%= f.submit "Submit", class: 'btn btn-success' %>
   </div>
<% end %>